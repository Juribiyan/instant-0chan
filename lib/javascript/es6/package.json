{
  "name": "instant-0chan",
  "version": "0.0.1",
  "description": "0chan revival kit",
  "scripts": {
    "build": "cross-env BABEL_ENV=production babel src --out-dir ../ --source-maps",
    "simplebuild": "babel src --out-dir ../ --source-maps"
  },
  "author": "Juribiyan",
  "license": "GPL-2",
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-env": "^1.7.0",
    "babel-preset-minify": "^0.4.1",
    "cross-env": "^5.1.5"
  }
}
